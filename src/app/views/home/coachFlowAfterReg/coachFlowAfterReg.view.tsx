import React, { Component } from "react";
import { connect } from "react-redux";

interface State {
    
}

interface Props {

}

class CoachFlowAfterReg extends Component<Props, State> {
    constructor(props: Props) {
        super(props)
    }
}


const mapStateToProps = (state: any) => ({
});

const mapDispatchToProps = (dispatch: any) => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(CoachFlowAfterReg);
